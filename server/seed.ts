// Seed data is now built into InMemoryStorage constructor.
// This function is kept as a no-op for backward compatibility with index.ts.
export function seedDatabase() {
  // No-op: InMemoryStorage auto-seeds on instantiation
}
